<Window x:Class="WpfApplication3.Window3"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Window3" Height="300" Width="300" Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            
            

        </Grid.ColumnDefinitions>
        <ListView Name="listShowData">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="姓名"  DisplayMemberBinding="{Binding Path='ChineseName'}"></GridViewColumn>
                    <GridViewColumn Header="姓名"  DisplayMemberBinding="{Binding Path='ChineseName'}"></GridViewColumn>
                    <GridViewColumn Header="姓名"  DisplayMemberBinding="{Binding Path='ChineseName'}">
                   
                    </GridViewColumn>
                    <GridViewColumn Header="姓名"  DisplayMemberBinding="{Binding Path='ChineseName'}"></GridViewColumn>
                    <GridViewColumn Header="姓名"  DisplayMemberBinding="{Binding Path='ChineseName'}"></GridViewColumn>
                    <GridViewColumn Header="姓名"  DisplayMemberBinding="{Binding Path='ChineseName'}"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
